:root{
  --bg:#f6f8fb;
  --card:#ffffff;
  --accent:#155ef0;
  --muted:#6b7280;
  --shadow: 0 6px 20px rgba(13,20,40,0.06);
  --radius:12px;
}

*{box-sizing:border-box}
body{
  font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  margin:0; background:var(--bg); color:#0b1220; -webkit-font-smoothing:antialiased;
}
.site-header{
  display:flex; align-items:center; justify-content:space-between; padding:18px 20px; background:linear-gradient(90deg,#fff,#fbfdff);
  border-bottom:1px solid rgba(10,20,50,0.04);
}
.brand{display:flex; gap:12px; align-items:center}
.logo{width:48px;height:48px;border-radius:10px;background:linear-gradient(180deg,#e8f0ff,#cfe0ff);display:flex;align-items:center;justify-content:center;font-size:22px}
.site-header h1{margin:0;font-size:16px}
nav a{margin-left:14px;color:#334155;text-decoration:none}

.hero{padding:60px 20px; text-align:left; max-width:1000px}
.hero h2{font-size:28px;margin:0 0 8px}
.hero p{color:var(--muted);margin:0 0 18px}
.hero-cta button, .btn-outlined{
  padding:10px 16px;border-radius:10px;border:none;background:var(--accent);color:#fff;font-weight:600;margin-right:10px;cursor:pointer;
}
.btn-outlined{background:transparent;border:1px solid rgba(16,24,40,0.06); color:var(--accent)}

.work{padding:20px; max-width:1100px}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:14px;margin-top:12px}
.card{background:var(--card);border-radius:12px;padding:8px;box-shadow:var(--shadow)}
.card img{width:100%;border-radius:8px; display:block}

.editor-panel{position:fixed;inset:40px;background:linear-gradient(180deg,#fff,#fbfdff);border-radius:14px;box-shadow:0 20px 60px rgba(10,20,60,0.2);display:flex;gap:12px;padding:12px;z-index:90}
.editor-panel[aria-hidden="true"]{display:none}
.editor-toolbar{display:flex;gap:8px;align-items:center}
.canvas-area{flex:1;background:linear-gradient(180deg,#f8fbff,#fff);border-radius:10px;padding:12px; border:1px dashed rgba(0,0,0,0.04); min-height:420px; position:relative; overflow:auto}
.editor-side{width:260px;background:#fff;border-radius:10px;padding:12px;box-shadow:var(--shadow)}

.canvas-item{position:absolute; touch-action:none; cursor:move}
.canvas-item img{display:block;max-width:100%;height:auto; border-radius:6px}
.canvas-text{display:inline-block;padding:6px 10px;background:rgba(255,255,255,0.9);border-radius:4px}

footer{padding:28px;text-align:center;color:var(--muted)}
.projects-list{margin-top:8px; max-height:220px; overflow:auto}
.projects-list button{display:block;width:100%;padding:8px;margin-bottom:6px;border-radius:8px;border:1px solid #eee;background:#fafafa; text-align:left}
