:root{
  --bg:#0f1724; --card:#0b1220; --accent:#f97316; --muted:#94a3b8; --white:#fff;
  --maxw:1100px;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}
*{box-sizing:border-box}
body{margin:0;color:var(--white);background:linear-gradient(180deg,#071020 0%, #0f1724 100%);-webkit-font-smoothing:antialiased}
.container{max-width:var(--maxw);margin:0 auto;padding:28px}
.site-header{backdrop-filter:saturate(140%) blur(6px);position:sticky;top:0;z-index:80;background:rgba(2,6,23,0.6);border-bottom:1px solid rgba(255,255,255,0.02)}
.header-inner{display:flex;align-items:center;justify-content:space-between}
.brand{color:var(--white);font-weight:700;text-decoration:none;font-size:20px}
.nav{display:flex;gap:18px}
.nav a{color:var(--muted);text-decoration:none}
.menu-btn{display:none;background:none;border:0;color:var(--white);font-size:22px}

.hero{padding:40px 0}
.hero-inner{display:grid;grid-template-columns:1fr 420px;gap:24px;align-items:center}
.hero-media{border-radius:12px;overflow:hidden;box-shadow:0 10px 30px rgba(2,6,23,0.6)}
.hero-video{width:100%;height:100%;display:block;object-fit:cover}
.hero-copy h1{font-size:36px;margin:0 0 8px}
.hero-copy p{color:var(--muted)}
.btn{display:inline-block;padding:10px 16px;border-radius:8px;text-decoration:none}
.btn.primary{background:var(--accent);color:#000;font-weight:600}
.btn.outline{border:1px solid rgba(255,255,255,0.06);color:var(--white)}

.section{padding:48px 0}
.section.alt{background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent)}
h2{margin-top:0;color:var(--white)}
.grid{display:grid;gap:16px}
.services-grid{grid-template-columns:repeat(auto-fit,minmax(220px,1fr))}
.card{background:rgba(255,255,255,0.03);padding:18px;border-radius:10px}

.gallery-grid{grid-template-columns:repeat(auto-fit,minmax(160px,1fr))}
.thumb{width:100%;height:160px;object-fit:cover;border-radius:8px;cursor:pointer;display:block}

.vids-grid{grid-template-columns:repeat(auto-fit,minmax(220px,1fr))}
.video-card{position:relative;border-radius:8px;overflow:hidden;cursor:pointer;background:#06101a;padding-bottom:8px}
.video-card img{width:100%;height:140px;object-fit:cover}
.video-play{position:absolute;left:10px;top:10px;font-size:18px;background:rgba(0,0,0,0.5);padding:6px;border-radius:6px}

.about-inner{display:flex;gap:18px;align-items:center}
.about-photo{width:240px;border-radius:10px;object-fit:cover}

.testimonials{grid-template-columns:repeat(auto-fit,minmax(240px,1fr))}
blockquote{background:rgba(255,255,255,0.02);padding:16px;border-radius:8px;color:var(--muted);font-style:italic}

.cta{padding:48px 0;text-align:center}
.cta-actions{display:flex;gap:12px;justify-content:center}

.site-footer{border-top:1px solid rgba(255,255,255,0.03);padding:20px 0;text-align:center;color:var(--muted)}

/* Modal */
.modal{position:fixed;inset:0;background:rgba(2,6,23,0.85);display:flex;align-items:center;justify-content:center;padding:20px;visibility:hidden;opacity:0;transition:opacity .18s}
.modal[aria-hidden="false"]{visibility:visible;opacity:1}
.modal-content{position:relative;max-width:900px;width:100%}
.modal-close{position:absolute;right:-6px;top:-6px;background:#111;padding:8px;border-radius:999px;border:0;color:var(--white);font-size:18px}

/* Responsive */
@media(max-width:900px){
  .hero-inner{grid-template-columns:1fr;grid-auto-rows:auto}
  .menu-btn{display:block}
  .nav{display:none;position:absolute;right:16px;top:60px;background:#071122;padding:10px;border-radius:8px}
  .nav.show{display:flex;flex-direction:column}
}
